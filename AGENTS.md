# 项目协作规则

## 项目与阶段

- 英文名：**Create: Feed Me Packages!**
- 中文名：**机械动力：喂我发包！**
- 工作目录：`D:\claude_sandbox\Create-Feed Me Packages!`。
- 平台：Minecraft **1.21.1 / NeoForge**。不自行增加其他游戏版本或加载器。
- 产品身份：Create 附属模组。DSP 是体验灵感，机制、表达和依赖选择以 Create 为先。
- 首个可玩版本只提供 Curios 饰品佩戴入口，暂时将 **Create 与 Curios 均作为必需依赖**；用户所说“机动”按本项目语境记为机械动力／Create。无 Curios 的护甲入口延后，不在首个可玩版本制造兼容分支。
- **依赖声明只保留 Minecraft／NeoForge 平台与 Create／Curios。** JEI、Package Couriers、Create: Mobile Packages 及其他兼容目标均不写入 `neoforge.mods.toml` 的 required／optional 依赖；实现兼容时用隔离模块和运行时存在性探测，缺失或版本变化不得阻止核心模组加载。
- 当前新增一条与旧实现隔离的 **`v0.1.0` 干净参考实现轨**。Scathiard 于 2026-09-05 明确要求 @Planner 作为技术组长，不参考已有版本，独立写出达到即将发布标准的 `0.1.0` 供长期执行者参考；该指令授权在 `reference/v0.1.0/` 内建立源码、构建配置、资源、测试与隔离运行实例，并为该实现设计干净 schema、注册 ID 和技术细节。源码隔离不改变连续测试编号：本次阶段交付仍使用 `release/v0.1.0-test.13/` 和文件名 `create-feed-me-packages-0.1.0-test.13.jar`。它不授权读取、复制、改写或反编译根 `src/`、根构建脚本、旧 `build/`、`run/`、`logs/`、`release/` 中的实现，也不授权删除、全局安装、系统配置、Git 推送、公开发布或其他父规则红线。
- plan-v2.0 的产品边界继续有效。参考实现可以为尚未拍板但必须落地的技术／交互细节选择一套明确方案并记录依据，供 Scathiard 与执行者评审；不得借“参考”删减已确认的 `v0.1.0` 功能或降低验证标准。根源码轨仍保持 R0 暂停，参考实现不得自动覆盖、合并或替换根工程。
- 2026-09-06 Scathiard 要求 @Planner 继续跟进数轮参考版本，并通过团队通信提交 test.13 实测反馈。继续迭代仍限参考轨；先处理物资复制报告，再按经确认的版本方案调整合成与交互。test.13 保留为可复查的已交付检查点，不再作为无阻断缺陷的稳定基线；不覆盖它，也不把后续插件或退货自动纳入当前修复。
- 2026-09-06 用户确认合并原 test.14／test.15 并推进：本轮为 `0.1.0-test.14`，包含创造存取复制修复、合成可撤销预留、固定背包优先、扩容链节编号与绑定光效；原 test.16 的整体交互／后台残包调整顺延为下一轮 test.15。授权只覆盖参考轨当前模型中的代码、资源、测试与新本地交付，不覆盖 schema 变更、迁移或根工程。
- 2026-09-06 test.14交付后，用户确认前轮修复及满背包收货通过；新增持物右键应只存入1个的修订，并要求暂时停用JEI ghost拖拽设过滤，保留JEI配方查看／填充。独立界面是后续研究方向，未指定替换当前背包入口的设计。版本顺序改为`v0.2.0`真实退货、`v0.3.0`插件构筑；退货不以前置插件系统为条件。这些范围变化进入下一轮规格，已交付test.14不覆盖。
- 2026-09-06 用户明确“t15启动”：授权参考轨实现唯一佩戴、后台残包、面板减负、右键单件存入及暂停JEI ghost，并验证后新增本地test.15交付。沿用当前schema；不覆盖test.14、不启动退货／插件／TaCZ消费、不修改根工程或用户实例。
- 2026-09-05 Scathiard 接受在当前会话继续参考实现：保留产品上下文，并承认 @Planner 曾误读根开发状态中的旧实现摘要；不再要求证明“完全未接触旧信息”。独立性改为可检查的工程边界：不读取或复用旧源码／旧构建实现，不把旧方案当默认答案；从已确认需求重新论证关键取舍，以实际第三方 API、独立实验和新测试验证，旧测试结果不计入本实现。发现误读须如实记录并评估受影响工作，不以自动更换会话作为唯一处理方式。根源码与产物隔离、发布门和父规则红线保持不变。
- 2026-09-04 用户确认缓存改用**精确组件物品身份**；该产品语义继续有效。其时另行授权并已经实现的 schema 1 → 2 迁移只属于未发布测试模型的历史事实，见 `docs/decisions/0001-精确组件物品身份.md`；2026-09-05 的预发布重构决定已经撤销它作为最终版本兼容合同的效力。
- 2026-09-05 用户确认重新设计缓存归属：普通**供应链坠**访问随该物品转交、毁坏后永久失去的本件缓存；**供应链坠·私人定坠**访问随玩家的个人缓存，并由普通坠子保留库存升级而来。已有个人缓存时只在服务端模拟证明全部库存与必要状态均可转入后原子合并，否则拒绝升级且不改变任一侧。任意等级均可私人化；后续高级私人定坠只携带一次待签收容量授权，第一名合格玩家成功认领后授权永久清除，转交已认领坠子不能重复给其他玩家解锁。永久容量升级必须体现 Create 序列组装，面板安装口不作为等级升级方式。补货箭头改按当前缓存实际缺口派生；满背包收件先试一个面板可见残包槽，其可见性待实机验证。现行产品口径已统一写入 `docs/项目计划书.md`，版本分配见 plan-v2.0 `docs/开发计划书/版本路线图.md`；受影响实现仍须等待详细规格、技术入口验证及另行取得干净新 schema 的实施授权。`test.12` 只保留为旧缓存模型检查点，不代表新规格。
- 2026-09-05 用户确认玩家插件系统的核心规则：**功能类／数值类只供策划分析，不是玩家可见分类**；游戏内只有一个通用插件舱和一种通用插件槽，任意合法插件均可进入任意槽，不设分类页、分类槽或分类配额。所有插件允许重复安装并逐件占槽；同功能重复件按“至少一件即生效”只计算一次，拆下一件但仍有同功能件时效果继续；数值重复件逐件进入统一公式并受硬上限约束。插件实体统一归当前缓存，普通缓存插件随 `cacheId` 与坠子转交，个人缓存插件随玩家保留；终端／缓存只作为内部效果作用域，不另建槽位或存储所有者。**1—5 级永久结构成长与可逆插件构筑是并行的两套增强，互不取代；某一目标的最高能力由 5 级与对应合法插件配置结合形成。1／2／3／4／5 级依次解锁 1／2／3／4／5 个通用插件槽。**等级升级只新增一个空槽，不生成插件；插件不能改写等级、增加槽位或跳过扩容链。插件制造体现 Create 加工，安装／拆卸及私人化合并使用服务端模拟与原子提交，降容不能截断或丢弃状态。具体插件、首发版本与配方仍待讨论，未授权实现。
- 本项目尚未发布首个正式版本，内部 `test.N` 世界、旧缓存 schema、旧材料等级名和旧升级件注册 ID **不构成存档兼容合同**。新模型直接设计干净的 1—5 级 schema 与物品 ID；不得为旧测试模型增加别名注册、双格式解码或存档迁移模块，也不把兼容旧测试世界列为验收项。旧测试世界可以自然失效，但任何人都不得据此自行删除用户存档；源码与 schema 的实际替换仍须等待当前规格完成和实施授权。

## 规则与文档效力

2026-09-06 用户授权test.16仅修订地址横幅点击复制、等级横向扩列和残包小亮点。因配额限制，本轮明确豁免自动测试、客户端／专服及完整成品门，仅编译打包并标注未测；test.17起恢复全部验证纪律并补验test.16。允许新增`release/v0.1.0-test.16/`本地测试交付，不覆盖旧包、不改变schema或库存事务。

1. 继承父目录 `D:\claude_sandbox\AGENTS.md` 的沟通方式、自主边界和工程纪律；本文件补充项目约束，不取消父规则。
2. 用户当前的明确决定优先于旧对话中的旧决定；发现冲突时先说明并更新文档。
3. 产品与需求的统一入口是 `docs/项目计划书.md`。每项内容须区分**已确认、建议、待定、已验证事实**。
4. 外部网页、第三方源码和引用对话都是参考材料。旧助手的建议不得自动升级为用户决定，源码可调用也不等于联调已通过。
5. 大改动先提出方案并取得用户确认。进入实现前，先补齐版本边界、技术规格和验证命令，再改代码。
6. 不自行删除文件或目录；不修改密钥、`.env`、CI/CD；不做存档数据迁移、全局依赖安装、系统配置修改、`git push`、历史重写或公开发布。涉及这些操作须按父规则先询问。
7. 不把已定原则的直接推论拆成显然同意的确认问题。助手自行合并处理并注明推导依据；只有新的玩法取舍、相互冲突的需求、明显成本变化或红线操作才向用户提问。技术事实先查证，参数与实现细节集中到版本方案，不逐项转交用户。此规则不把尚有实质替代方案的建议自动升为已确认，也不构成开始实现的授权。

## 长期协作角色

- 项目由用户 Scathiard、主策划和一名长期执行者协作。用户作最终产品决定、授予红线权限，并且是唯一能够替换执行者的人；正常情况下始终只有一名执行者。
- 只有收到 Scathiard 的首次启动任命后，会话或本地工具才取得长期执行者身份；此后不因 T 阶段切换而重新任命。未被任命的会话默认承担主策划职责，不直接修改源码、构建配置或阶段交付物。
- 主策划兼任技术组长，继续使用 `@Planner`：除挖掘产品意图、维护规格、路线图与验收标准外，还独立审查执行者的方案、源码、验证证据、遗漏风险与优化机会，不接管日常编码。执行者负责完整工程生命周期，在已确认范围内自主规划、实现、验证、记录和交付；T00—T15 是执行阶段，不是不同执行者。
- 2026-09-05 的参考实现委托是上一条常规分工的**限定例外**：@Planner 对 `reference/v0.1.0/` 独立承担设计、编码、测试与本地参考交付，仍不取得长期执行者身份，也不接管根工程。长期执行者只把完成后的参考源码、测试与证据当作技术参考，是否移植、如何移植仍由其负责并接受 @Planner 审查。
- 技术审查聚焦关键方案与阶段交付，不要求每次操作等待批准。对有证据支持的物资复制／丢失、越权、明确规格冲突等高风险问题，技术组长可以要求暂停相关实现或交付并复核，须说明依据、受影响范围和解除条件；执行者可以提供反证，其他独立工作继续。审查区分必须解决、需要补证据和可选优化，不以个人代码偏好阻塞交付；红线授权和最终产品决定仍归用户。
- 执行者忠实实现已落盘的产品意图，不负责猜测隐含需求。实现偏离规格属于工程问题；实现符合规格但体验仍需调整时，由用户与主策划修订规格后再交给执行者。
- 普通技术细节由执行者自行处理。涉及新玩法取舍、规格冲突、明显范围或成本变化以及父规则红线时，只暂停受影响部分并反馈；无关且安全的工作可以继续。
- 三方通过 `docs/团队通信.md` 追加消息。主策划和执行者发送的新消息明确写出发信人与收件人；用户可以只写 `**用户：**` 和自然语言正文，无需编号、时间戳或其他字段。用户明确 `@Scathiard`、`@Planner`、`@Executor` 或 `@All` 时按收件人处理，未写 `@` 时视为全员可见并按职责分流；`@` 只负责路由，不扩大任何人的权限。用户暂时负责提醒其他成员读取最新消息。
- 团队通信只同步需要其他成员知晓或处理的变化，例如影响其判断或后续工作的产品／范围／权限变更、需要协调的阻塞与决策、可交接的阶段成果。日常编码、调试、构建、测试轮次、截图和一般文档维护不单独发信，相关细节按需留在开发状态或技术记录。相关变化合并成一条简短消息并链接详情；没有需要同步的变化就不发，不按完成动作的次数播报。
- 长期执行者可以使用临时子执行者完成有边界的研究、复核、测试或局部实现，但不得把产品决定、对外承诺或最终责任转交出去，也不得建立第二名长期执行者。
- 本节只固定核心边界。具体消息状态、报告格式、任务粒度和工程流程可以根据真实协作摩擦逐步调整，不要求初版协议覆盖所有情形。

## 目录与命名约定

先维护下表，再创建新目录。当前只建立文档需要的结构，不预建空的工程目录。

| 路径 | 用途与约定 | 保留与清理 |
| --- | --- | --- |
| `AGENTS.md` | 当前项目规则；规范改变时先修改此文件 | 长期保留，不自动删除 |
| `README.md` | 项目身份、阶段和阅读入口；不复制详细需求 | 随阶段更新 |
| `docs/项目计划书.md` | 产品定义、已确认需求、首版边界、成长参数、技术验证、阶段与验收计划 | 作为当前产品入口持续维护；开发计划不得暗改其中已确认规则 |
| `docs/项目计划书-plan-v1.0-历史.md` | 2026-09-05 重写前的旧总计划快照；保留旧玩家单缓存、材料阶段与 T00—T15 语境供追溯 | 历史只读，不再作为实现依据，不覆盖或删除 |
| `docs/开发状态.md` | 当前任务、授权边界、实际命令与验证证据；只记录已执行事实 | 每次任务状态或验证边界变化后更新，不用计划冒充结果 |
| `docs/执行者宪章.md` | 唯一长期执行者的稳定职责、自治边界、验证责任和交接原则 | 作为可迭代的工作契约长期保留；不复制易过期的任务进度 |
| `docs/执行者启动提示.md` | 首次任命长期执行者时可直接使用的短 prompt，以及恢复同一执行者的方法 | 随协作经验迭代；当前任务仍从权威文档和通信记录读取 |
| `docs/团队通信.md` | 用户、主策划与执行者的追加式群组记录；承载消息、分流、问题、答复和交接，不替代产品规格或开发状态 | 只在文件末尾追加；纠错另发新消息，不删除、改写或重排历史；计划纳入本地 Git 历史 |
| `docs/开发计划书/` | 可执行开发计划总目录；`README.md` 为索引，另含版本路线图、架构设计、非功能需求、测试策略、任务拆分、变更记录 | 本阶段按实际内容建立；与产品计划冲突时先修正开发计划，不反向覆盖产品决定 |
| `docs/开发计划书/版本路线图-plan-v1.0-历史.md` | plan-v2.0 重排前的旧版本路线图快照；记录旧 M0—M6 与后续版本分配 | 历史只读，不再作为排期或验收依据，不覆盖或删除 |
| `docs/开发计划书/specs/` | 首个可玩版本各功能规格；文件名使用两位序号加中文主题，如 `01-个人缓存.md` | 每个首版功能必须有输入、状态、事务边界、错误处理和验收条件；后续功能只在路线图登记 |
| `docs/research/` | 需要独立保存的调研，文件名 `YYYY-MM-DD-主题.md`；必须记来源、核实日期和证据边界。对抗式审查须区分反例、源码事实、推断、建议防线和待执行验证 | 按需建立；过期内容标注失效，不自动删除 |
| `docs/decisions/` | 获用户确认后才建立的关键决策记录，文件名 `NNNN-主题.md` | 不把未确认建议写成决策 |
| `reference/v0.1.0/` | @Planner 的 `v0.1.0` 干净参考实现；独立 Gradle 工程、源码、资源、测试、运行说明与实现决策均放此处；只读 plan-v2.0、公开／本地第三方 API 与官方资料，不读取或复制根工程实现 | 长期保留供执行者逐模块参考；不得自动覆盖根工程，不从旧 JAR／源码抽取实现 |
| `../.fmp-reference-build-safe/` | 干净参考实现的可再生 Gradle 输出与隔离运行目录；避开项目名 `!` 导致的 SecureJar 路径问题 | 不纳入交付；清理仍须用户授权 |
| `settings.gradle`、`build.gradle`、`gradle.properties`、`gradlew*`、`gradle/` | NeoForge 21.1.219 / ModDevGradle 工程、Wrapper 与锁定依赖；不写机器私有绝对路径 | 实施阶段使用；修改依赖先更新计划与状态记录 |
| `src/` | Java 源码、资源、模板、数据生成资源与测试源；根包固定为 `dev.scathiard.feedmepackages` | 按职责分包，不把第三方源码或资产拷入 |
| `../.fmp-build-safe/` | 本项目的 Gradle `buildDirectory`。项目目录名中的 `!` 会被当前 SecureJar／union URI 运行链误判为归档边界，使生成的 NeoForge JAR 无法被发现；构建脚本只用相对路径指向该相邻目录，不写机器私有绝对路径 | 只放可再生构建输出，不纳入交付；清理仍须用户授权 |
| `build/`、`.gradle/`、`run/` | `build/` 保留切换安全生成目录前的旧输出与诊断产物；`.gradle/` 为项目缓存，`run/` 为隔离测试实例；不使用玩家真实存档测试 | 不纳入交付；新 Gradle 输出不得再写入 `build/`；清理仍须用户授权 |
| `release/vX.Y.Z-test.N/`、`release/vX.Y.Z/` | 前者放阶段测试 JAR，后者只放达到版本完成门的最终本地交付；两者均须附依赖说明、变更说明和验证记录 | 本地交付不等于公开发布；递增 `test.N`，不得覆盖旧版 |
| `release/v0.1.0-test.13/` | 本次干净参考实现的连续阶段交付；文件名固定为 `create-feed-me-packages-0.1.0-test.13.jar`，来源与验证说明写入同目录文档，不把 `reference` 加进文件名 | 不覆盖旧 release；只有通过参考实现完整发布门后才建立，不等于公开发布 |
| `release/v0.1.0-test.14/` | 已授权的合并修订交付，文件名 `create-feed-me-packages-0.1.0-test.14.jar`；含范围、依赖、变更、验证和测试说明 | 修复与版本回归通过后建立；不覆盖 test.13，不等于公开发布 |
| `release/v0.1.0-test.15/` | 当前交互修订交付，文件名 `create-feed-me-packages-0.1.0-test.15.jar`；含依赖、变更、验证和玩家测试说明 | 完成本轮发布门后建立；不覆盖任何旧交付，不等于公开发布 |

- 文档使用 UTF-8、中文；代码、命令、资源 ID 与变量名使用英文。
- plan-v2.0 沿用已固定的 `mod_id=create_feed_me_packages`、Maven group `dev.scathiard`、Java 根包 `dev.scathiard.feedmepackages`、首版 `0.1.0` 和 MIT 代码许可证。修改这些标识须先更新开发计划和影响分析。
- 第三方源码与游戏资产不直接拷入项目；引用或复用前核对许可证，并保留必要声明。
- 调研用临时文件必须有明确用途与位置；不在根目录堆放下载物、日志和截图。
- 2026-08-30 已实测：默认 `build/` 下的 NeoForge JAR 内容完整且在 Java classpath 中，但 FML 不发现 `neoforge`；仅把 `buildDirectory` 改到不含 `!` 的 `../.fmp-build-safe/` 后，同一配置可加载 NeoForge、Create、Curios 与本模组并启动专服。除非后续工具链修复并重新通过 C0，不得把生成目录改回项目路径内。

## 产品约束

以下只保存高频、不可越过的产品边界；完整体验、状态机、开放问题与版本范围以 `docs/项目计划书.md` plan-v2.0 为准。

- 物流缓存持有真实物资。过滤、最低／最高数量、ghost 模板和请求记录都不能生成物资。
- 普通**供应链坠**通过稳定 `cacheId` 访问本件缓存；本件缓存的库存、格子、阈值、请求／收据／残包、等级和插件整体随坠子转交。最后一个合法入口真正毁坏后，玩家永久失去访问，不提供恢复玩法；异常同 ID 副本只能访问同一份缓存。
- **供应链坠·私人定坠**按当前佩戴玩家访问个人缓存；终端转交只转交网络绑定，不转移原玩家缓存。普通坠子私人化必须保留整份缓存；已有个人缓存时先模拟全量合并，任一状态无法容纳则整体拒绝。
- 每件终端只绑定一个 Create 网络，绑定随终端转交。2026-09-06 用户改为每名玩家最多佩戴一件供应链坠，两类坠子共用这一限制；即使其他模组增加项链槽也拒绝第二件，不再提供逐件禁用或终端管理面板。异常绕过佩戴限制时服务端拒绝业务，不能暗选缓存或丢弃坠子；卸下多余坠子后恢复。绑定／清绑参考当前 Create 原生交互，绑定成功的坠子显示附魔光效。
- 有效佩戴但未绑定时仍可访问缓存并处理目标明确的迟到包，只是不发新请求；清绑或换网不清缓存、残包和请求记录。界面显示服务端生成的专用投递地址并提供复制按钮。
- 首个可玩版本只提供 Curios 项链槽；正式名称为**供应链坠 / Supply Chain Pendant**与**供应链坠·私人定坠 / Supply Chain Pendant: Made to Order**。供应链甲／供应链盔后置。
- 未佩戴有效终端时，物流面板、缓存存取、配置、补货、收包、自动取料及未来退货均不存在。状态保留，重新佩戴后先续收残包／已有包，再检查补货。
- 死亡本身不删除缓存。普通缓存继续随坠子实体，个人缓存随玩家；坠子与缓存外物品仍遵循原有死亡、掉落和世界规则。
- 缓存只统计自身现货，不统计原版背包、快捷栏、副手、请求记录或远端库存。取出／真实消费即减缓存，主动存回即增缓存。
- 每格最低值用于补货、最高值用于未来退货：低于最低值补到最低值，高于最高值退到最高值；等于阈值不发空操作。最高值不是实际容量。
- 补货箭头按当前缓存缺口派生：无缺口不显示；有缺口且已有请求记录显示绿色下箭头；有缺口且未成功请求显示灰色下箭头。库存补足后箭头消失，但请求记录不自动删除。
- 不追踪外部载具或判断包裹是否丢失，不自动超时重发、赔付或恢复。按格手动重置只清请求记录，不增加物资或删除真实旧包；迟到包仍按实物处理。
- 缓存身份为物品注册 ID 加完整持久化组件，数量归一为 1。带组件物品允许进入且原样输出；同 ID 不同组件可分别占格，同一精确变体不能重复设格。无法安全往返、禁止存储或规范模板超过 1,024 字节时整体拒绝。
- 从背包存入实物时设置过滤并转入真实库存；持物左键尽量存入当前堆栈，右键只存1个，剩余留在光标。JEI ghost拖拽设过滤暂时停用，配方查看与填充保留；恢复ghost须另行通过交互方案和真实客户端验收。有现货的格必须先取空才能清除或改过滤。取出必须进入合法光标／背包位置，不能因一组以下的数量直接丢到世界。
- 面板在生存和创造均可用。格子尺寸与间距对齐原版物品栏，小点放在格内，只展开该格下方的紧凑滑条；物品、数量、箭头和小点不得互相遮挡。面板不常驻显示等级，不提供取料优先级、残包取回或终端管理按钮；地址复制使用紧凑的 Create 交互。减少常态长 tooltip 和层级，只在必要操作或异常时给出简短反馈。美术体系重做另议，当前先验证功能与操作。
- 已开放物品进入独立创造模式标签页，不能依赖 `/give` 作为正常取得入口。
- 满背包收件使用专用服务收件口，不扩展永久隐形背包。能完整拆空时立即入库；部分入库时每缓存先保留一个真实残包。2026-09-06 用户要求残包在后台自动处理，不设玩家取回按钮或需要手动排障的流程；隐藏表现不能删除真实余货或诊断，满位与失效过滤的自动处置必须证明守恒。
- 请求与包裹必须固定目标缓存、玩家、请求 ID、精确变体和过滤修订；只靠 `FMP@玩家名` 不足以区分多缓存。令牌承载是 plan-v2.0 G-02，无法验证目标时保留原包，不能猜当前缓存。
- 正式等级只称 1—5 级，依次为 9／128／1、16／256／2、24／512／3、30／1,024／4、36／2,048／5（物流格／单格容量／插件槽权益）。旧石／铁／安山／黄铜／坚固板名称不再定义等级或物品 ID。
- 等级归缓存，永久升级必须经过 Create 序列组装与中间产物。随机性只能作用于可重制中间产物，不能随机损毁装有缓存的坠子；预览和模拟不得升级缓存或消费授权。
- 五级缓存对应四次扩容，玩家看到的扩容链节按第 1—4 次升级编号，分别用于 1→2、2→3、3→4、4→5；不得把目标等级 2—5 当作链节序号。私人化链节不计入四种扩容链节。
- 2026-09-06 用户确认制造范式：Create序列组装制造链节，锻造台完成坠子升级与私人化。坠子在制造菜单中时允许这一项最终装配，不授予未佩戴时的缓存存取、收包或取料能力。五级投入须显著陡增，主要体现为关键材料的获取难度与加工链复杂度，不能仅靠材料数量倍增、同工序反复循环、降成功率或固定等待时间充数。具体配方是参考实现的明确选型，仍须验证与实机平衡；根工程R0暂停不变。
- 任意等级均可私人化。后续高级私人定坠只携带一次待签收容量授权；第一名合格玩家成功认领后授权永久清除，已认领坠子不能靠转交重复解锁。
- 插件与等级是两套增强。游戏内只有通用插件舱和通用槽；所有插件可重复且逐件占槽，同功能重复件按存在性生效，数值件逐个进入固定公式和硬上限。插件随当前缓存；安装、拆卸、私人化合并均须模拟后原子提交，降容不得截断或丢物。
- `v0.1.0`与`v0.2.0`只保存1—5级插件槽权益，不显示空插件舱或注册占位插件；首批插件在`v0.3.0`经产品门确认后开放。
- 玩家直接用手使用物品仍先取出真实堆栈。自动取料长期目标是通用“某操作消费另一物品”协议；`v0.1.0` 保障原版 2×2／3×3、配方书、JEI 配方填充及标准投射物链，TaCZ 后置。查询、预览和取消不得扣料，真实转移／消费成功才扣。
- 自动取料固定背包优先，不显示也不保留玩家手动切换优先级的入口；背包不足时由缓存补足。未来改变优先级的插件仅为候选，不因这次讨论自动进入首批插件；即使以后提供，也不因此恢复面板开关。
- 补货只向 Create 提交真实请求并在玩家侧接收完整专用包；纸飞机与运输蜂按原生方式运输。两者都须真实联调，但不写入依赖元数据，不由 FMP 接管寻路、定位或成员资格。
- 退货位于`v0.2.0`，不依赖先实现插件：使用运输物格和返回地址，不能出发时不打包、不扣缓存或载具。蜂格原蜂不消耗，派出一次性蜂；成功或失去目标终止时销毁临时蜂，失败原包在当地掉落。先沿用原生换站，不预先实现防串网。
- 不自建全局配送系统、不新增退货站、不用瞬移扣库代替包裹、不自动赔付、不为未发布 `test.N` 实现兼容／迁移模块。

## 实现时必须守住的边界

- 服务端先校验唯一有效佩戴和活动缓存，再接受常规缓存、请求、取料、插件或退货意图；异常多件、旧菜单和延迟请求不能绕过，不以“已移除管理界面”代替权限校验。已确认的锻造制造入口改为校验实际制造菜单、玩家、输入坠子、配方、目标缓存与输出位置，不要求该输入同时被佩戴，也不能借此调用其他未佩戴业务。
- 所有业务通过统一 `CacheHandle` 访问缓存，不能继续把缓存固定为玩家附件。客户端提交的玩家 ID、`cacheId`、数量、模板和修订均不能直接作为权威事实。
- 使用不可变 `ItemVariantKey` 统一过滤、库存、请求、收据、包裹与消费。摘要／哈希只可辅助日志或索引，最终匹配必须比较完整身份；对外 `ItemStack` 使用防御性副本。
- 本件缓存注册、私人化、容量授权、插件拆装和残包处理必须有模拟与原子提交边界。失败不允许部分迁移、掉落补偿、默认清洗或静默截断。
- 自动解包按成功转入量同步扣减包裹；缓存增加量等于包裹减少量。重复事件、部分包、迟到包、手动重置和保存重载不得造成二次入库或负请求记录。
- 服务收件口只接管合法专用包。残包槽不接受普通物品；新包无法完全拆空且残包槽已占用时，拒绝接管并让原路径保留实物。
- 普通背包、光标、合成格、包裹和第三方接口的转出均遵守物品原生合法堆叠上限，不能硬编码 64。
- 合成、批量合成、余物返还、投射物和取消操作均须守恒；结果或余物没有安全位置时停止提交。
- UI 必须覆盖生存／创造、配方书、JEI、不同 GUI 缩放和滚动；隐藏界面不能替代服务端停用。
- 可选兼容以独立模块和运行时探测实现；缺失或版本变化不得阻止核心加载。每项兼容记录实际版本、入口和验证边界。
- 新 schema 只验证当前模型的创建、保存重载、多人隔离和坏数据拒绝；不添加旧测试迁移夹具、别名或双格式读取。

## 验证纪律

### 文档阶段

- 用 PowerShell `Get-Content -Encoding UTF8` 回读所有新增或修改文档。
- 检查本地 Markdown 链接、目录表、平台、项目名称和需求状态是否一致。
- 确认没有把建议写成已确认，也没有创建未经批准的源代码、构建配置或依赖目录。
- 文档核对只能报告为文档验证，不得称为编译、游戏内运行或模组兼容验证。
- 对抗式审查中的方案仍是建议；与后续用户决定冲突的内容须标注被替代。退货已经恢复研究，不能继续沿用整项搁置的旧状态，也不能把研究结论当成已实测兼容；未确认成本不因批注或审查自动生效。正式等级名 1—5、每级格数／容量／插件槽，取料默认值、按格重置、非空格编辑、供应链系列命名、Curios 首版范围、单物品单格、饰品单网络及多网冲突不再标为待决定。

### 实现阶段

- 工程命令以 `docs/开发状态.md` 的“已验证命令”表为准。当前机器须在命令进程内选择 Minecraft `java-runtime-delta` 的 Java 21；当前网络还须用进程级 `GRADLE_OPTS` 传递 `127.0.0.1:7890` HTTP／HTTPS 代理，不能把该机器私有代理写入项目或系统配置。
- T01—T04 已验证的常规入口：`.\gradlew.bat test build runGameTestServer --no-daemon --no-configuration-cache --console=plain`；`.\gradlew.bat runServer --no-daemon --no-configuration-cache --console=plain`（到达 `Done` 后输入 `stop`，须正常保存退出）；`.\gradlew.bat runClient --no-daemon --no-configuration-cache --console=plain`（当前只验证稳定启动，人工中断不算正常退出测试）；`.\gradlew.bat dependencies --configuration runtimeClasspath --no-daemon --no-configuration-cache --console=plain`。T03 跨 JVM 玩家文件回读另用 `.\gradlew.bat runGameTestServer -PfmpRequirePriorPlayerdata=true --no-daemon --no-configuration-cache --console=plain`，此前必须已有一次普通 GameTest Server 成功写入固定测试 UUID。完整前置命令、退出码、当前测试数量和日志见 `docs/开发状态.md`。
- 必需依赖拒绝探针使用 `gradle/verification/exclude-runtime-mod.init.gradle`，按加载器明确的缺失诊断文本验收；NeoForge 捕获加载失败后 Gradle 仍可能返回 0，不得只以进程退出码判定。未经成功执行的目标命令仍不得写成有效命令。
- 至少验证：出入库守恒、重复请求与重复本地收包事件、部分／迟到包裹、满库存、精确变体存取与同 ID 不同组件隔离、超大／不可解码模板拒绝、未授权操作、卸下与掉落重装、当前干净 schema 保存重载、多人隔离。旧测试 schema 迁移不属于最终验收。
- 合成专项至少覆盖：原料不足、不同来源混用、批量操作、结果放不下、桶等配方余物、操作中卸下终端，以及界面关闭。验证固定背包优先、背包不足时缓存补足、总量不足不透支、未佩戴停用；不得只改按钮而让旧偏好或旧网络意图继续改变实际顺序。新增“请求64原木→JEI合成一次木板→结束合成”用例，核对缓存、合成格、产物和实际请求数量；待用下一轮原料不能造成额外采购。
- 弹药专项至少覆盖：普通弓、弩，背包无箭但缓存有箭，查询与取消蓄力不扣料，弩成功装填后不在发射时重复扣料，无限／创造与多重射击按原规则计数，中途卸下和服务端校验。TaCZ 仅在确定兼容版本与数据策略后验收不同弹种、装填、弹匣、背包直读及不修改枪包配置；未验证则明确报告未支持。
- 新决定专项至少覆盖：同物重复过滤不能建立；每件饰品最多一个绑定、允许清除、不同饰品网络独立；换网不清当前缓存待收，多件不重复发单；Curios 供应链坠作为首个可玩版本唯一佩戴入口；1—5 级分别为 9／16／24／30／36 格、128／256／512／1,024／2,048 单格容量和 1／2／3／4／5 个插件槽；普通缓存整份随坠子，个人缓存整份随玩家。
- 缓存与滑条专项至少覆盖：取出就扣缓存、背包数量不影响补货、持物左键按容量存入及右键只存1个、存回超过上限、补回最低／退回最高的差额、JEI ghost停用时拖拽不改过滤／库存、两端初始关闭、单方向启用、阈值等号不发货、实际容量不足、有在途订单时反复触发检查、拖动／滚轮／数字精调，以及非空格不能改过滤。
- 新边界专项至少覆盖：按格重置不增加物资且不删除旧包、重置后迟到包仍可处理、只拆目标玩家与目标缓存明确的专用包、带组件物品不被静默转换、同 ID 不同组件不串格／串单、组件实物完整往返、首版补货下箭头与残包状态，以及连续标准分包。新模型只验证当前schema的首次创建、保存重载与坏数据安全拒绝，不编写旧测试schema迁移夹具；红色／灰色上箭头随`v0.2.0`退货验收，不能把后续显示约定写成首版已通过的发运测试。
- 部分入库专项至少覆盖：恰好装满实际容量、完全无空间、可全部收下、混合内容、重复本地收包事件和保存重载；缓存增加量与包裹减少量相等，未收内容仍在原包裹，不能按退货阈值截断入库。
- 残包续收专项至少覆盖：腾出空间后自动转入，实际转入后再判定补货，残包足够时不为同一缺口新发单、不足时只处理剩余缺口；普通散装物品及未标记包裹保持原样，未转入内容不计作缓存现货。
- 缓存归属专项至少覆盖：普通坠子转交后完整访问同一 `cacheId`，私人定坠转交后双方个人缓存不转移；普通缓存最后合法入口毁坏、相同 `cacheId` 异常副本、私人化成功／失败、既有个人缓存全量合并、一次性授权认领、多人隔离和保存重载。等级与插件槽权益归当前缓存，多件终端不得叠加或复制能力。
- 死亡专项至少覆盖：佩戴与未佩戴时死亡、世界“死亡不掉落”开／关、连续死亡重生及保存重载；普通缓存继续随坠子实体，个人缓存继续随玩家，死亡本身不额外清空、复制或重发。普通物品栏、坠子实体、缓存外包裹与真正毁坏的本件入口仍按各自规则处理。
- 佩戴开关专项至少覆盖：仅携带不激活；卸下后界面与操作入口消失，不能存取缓存或自动取料、不新发补退货、不拆新包或续收残包；原版背包操作不受影响。重装先收已有专用包裹再判定补货，反复穿脱不清待收记录或重复发单；卸下瞬间的配方与延迟操作不能绕过服务端校验。
- 分别报告自动测试、客户端启动、独立服务器和游戏内人工测试；未执行的项目须明确列出。
- 本轮新增专项至少覆盖：纸飞机／运输蜂分别及共同安装；JEI 原版配方填充；Curios 必需依赖、两类坠子的第二件佩戴拒绝、异常强制多件安全拒绝及卸下后恢复；固定背包优先、旧切换意图拒绝、按格重置；背包全满时服务收件口完整／部分／拒绝三条路径；后台残包续收和安全处置，不依赖手动按钮。护甲拆卸不属首个可玩版本验收；退货进入后续实验时另测无载具、目标无效、配置禁用、满站、受理失败不扣物、途中满站以及蜂和纸飞机不同的载具去向。
- 一次性蜂专项至少覆盖：运输模组存在／缺失、格中原蜂数量始终不因发运减少、临时蜂交付后不入港回收、敲击等原生路径不产出普通蜂、交付失败包裹守恒、满站与区块卸载的分别处理、实体与虚拟运输记录同时终止，以及保存重载／玩家离线或卸下后的策略保留。原生换站与串网仅列实际游玩观察项，不以“必须不改投”为验收门槛；普通蜂行为不得被本模组的临时模式一并改写。

