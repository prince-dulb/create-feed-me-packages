# v0.1.0-test.3 变更说明

本构建包含 `v0.1.0-test.2` 的依赖声明修订，并修复创造模式入口：

- 新增独立的“机械动力：喂我发包！”创造模式标签页。
- 标签页以供应链坠为图标。
- 标签页按顺序提供供应链坠、铁质、安山、黄铜、坚固缓存升级件，共五件物品。
- 新增真实注册表 GameTest，避免物品注册成功却没有创造模式入口再次发生。

这是 `release/v0.1.0-test.2/` 之后的新测试构建；旧目录保留作为历史产物，测试时请只使用本目录中的 JAR。
