# Create: Feed Me Packages! 0.1.0-test.23 — 验证记录

## 构建环境

- Java: OpenJDK 21.0.7（`java-runtime-delta`）
- Gradle: 8.12, NeoForge 21.1.219
- 代理: `127.0.0.1:7890`（进程级 `GRADLE_OPTS`）

## 变更摘要

残包模型从「每份缓存一个残包槽」改为「每个物品格一个残包位」（按精确变体分格，不同物品格的残包互不阻塞）；持久化 schema 从 5 升至 6，并提供 2/4/5→6 迁移。同时精简了成长（锻造）链与面板的提示文字。

## 自动测试

### JUnit（2026-09-07）

```
.\gradlew.bat test build runGameTestServer -PfmpGameTests --no-daemon --no-configuration-cache --console=plain
```

- JUnit：**15/15 通过**

### GameTest（2026-09-07，新世界 `-model7`）

```
.\gradlew.bat runGameTestServer -PfmpGameTests --no-daemon --no-configuration-cache --console=plain
```

- GameTest：**85/85 通过**（含新增 `perCellResidualsDoNotContend`：两不同物品格残包并存、吸收一格不影响另一格、逐格残包保存重载正确回读）

### 跨 JVM 回读（2026-09-07）

```
.\gradlew.bat runGameTestServer -PfmpGameTests -PfmpRequirePriorPersistence=true --no-daemon --no-configuration-cache --console=plain
```

- GameTest：**85/85 通过**，真实玩家文件 / Curios / schema 6 缓存账本（含逐格残包、一次性授权）在前后 JVM 间正确回读。

## JAR 校验

```
文件: create-feed-me-packages-0.1.0-test.23.jar
大小: 301,693 字节
SHA256: FAE6F3834A73C068FCC5EDC2F437ABFD107D923864FC877BA993B351BF9A4957
```

## 未执行项目

- 成品客户端截图门、缺依赖探针：未执行（沿用 test.17 起记录的缺口，建议随下一轮补验）。
- 独立专服人工 `Done`+`stop`、真实客户端启动：未执行。GameTestServer 已覆盖世界加载与保存重载；如需等价专服验证可另行补跑。

> 说明：旧 `-model2` 测试世界含 schema 5 数据（含测试刻意剥掉封签的残包）。按「不为旧测试世界建迁移、schema 变更即重开世界」的既有约定，本轮改用全新 `-model6`/`-model7` 世界；旧目录保留未删。
