# v0.1.0-test.6 验证记录

- **打包日期**：2026-09-02
- **状态**：T06／T07 收包侧实现并通过自动回归；T05 五项界面修复待人工复测；纸飞机／运输蜂联调（T08／T09）未开始
- **JAR 大小**：285,718 字节
- **SHA-256**：`27d033d44e5deb4ea8181cf2d6d6c52166015de8184c48c00816462d1bd02357`

| 验证 | 结果 |
| --- | --- |
| `gradlew test build runGameTestServer` | 成功；72/72 JUnit、39/39 GameTest（29 项既有回归全部保持通过 + 10 项 T07 新测试） |
| GameTest 稳定性复跑 | `runGameTestServer` 连续 3 次 39/39 通过（收包测试依赖真实 Create 包裹组件与时序，复跑排除波次编排偶然性） |
| 严格跨 JVM 玩家文件回读 | 成功；`-PfmpRequirePriorPlayerdata=true` 下 39/39 GameTest 通过 |
| 专服启动 | 到达 `Done (1.512s)`；`help fmp` 显示 `/fmp cache-audit [<player>]`；`stop` 正常保存退出 |
| 收包守恒 GameTest | 整包入库（32 进 32 结算）、部分入库（128 容量留 12 残包）、腾位续收（取出 30 后残包 12 续入，待收不再二次结算）均通过 |
| 拒收与隔离 GameTest | 畸形堆叠（128/64）整包拒绝且无收据、外来收据整包不动、他人地址不理会、特殊组件石头留包、无待收迟到包正常入库、未佩戴不扫描、PREPARED 日志封锁自动收包 |
| 修复的测试隔离缺陷 | `t04SneakUseReadsRealStockLinkNetworkAndAvailability` 等三个 T04 测试的 Stock Link／Stock Ticker／Redstone Requester 行为会通过 Create 全局 LINKS 缓存跨波次泄漏，使 `t04CuriosAccessMatrix` 的“网络不可用”断言随测试波次编排偶发失败；已在三个测试结束前移除方块并调用 `LogisticallyLinkedBehaviour.remove()`。属测试基建修复，不断言、不改变任何产品行为 |
| JAR 元数据 | 4 个 required：NeoForge、Minecraft、Create、Curios；0 个 optional（`ResourceContractTest` 固定） |
| 公共主类隔离 | `DedicatedServerClasspathTest` 通过：无 `net/minecraft/client`、无 `mezz/jei` 引用 |

## 已知缺口（不阻塞本构建的人工测试）

- `SPK-PKG-01` 的四个受控中断切点（PREPARED 后、包裹减少后、缓存增加后、COMMITTED 后）重载守恒注入测试尚未执行；事务日志与加载封锁机制已就位，切点验证是 T07 完成前的剩余工作。
- 纸飞机（C2）与运输蜂（C3）真实运输联调未开始；当前到达包裹来自 T06 已验证的真实 Create 打包机链。
- 手动拆包一个已被本模组收据标记的残包会得到散装物品（遵循原版规则，不自动计入缓存）；自动收包路径通常先于手动拆包处理专用包裹。

## 请人工复测（可与 test.5 的五项界面复测同轮进行）

1. 沿用 test.4 的真实场景：让 Create 打包机向 `FMP@玩家名` 发一个实体包裹，确认包裹进入背包后约 1 秒内自动拆入缓存（此前需要手动拆包且箭头不消失）。
2. 确认绿色“待收”下箭头在包裹实际入库后消失；若包裹内容因缓存满而剩余，确认包裹保留在背包中且箭头不再等待已入库部分。
3. 从缓存取出部分物资，确认背包中残包余货在腾出空间后自动继续转入。
4. 对一个普通地址（非 `FMP@`）包裹重复上述操作，确认本模组完全不干预。
