# v0.1.0-test.11 验证记录

- **构建日期**：2026-09-05
- **构建产物**：`create-feed-me-packages-0.1.0.jar`
- **SHA-256**：`f03b886147bc16df88f211e1c5de7626f21601b74b8998e97790579dc14c5147`
- **基础**：`test.10` 之上增量修复，数据格式与 `test.10` 完全一致（schema 2）

## 自动验证

| 验证项 | 结果 |
| --- | --- |
| `test build runGameTestServer`（JEI 基线 19.21.0.246 + 本轮修复） | 退出码 0；72/72 JUnit；**69/69 GameTest** |
| 新增 `t06RealDeliveredPackageAutoUnpacksIntoCache` | 通过：真实打包机产出 `FMP@玩家` 包裹 → 放入背包 → `scanNow` 自动拆入缓存 32 → 待收清零 → 包裹壳移除 |
| JEI 完整版 19.21.0.246 开发实例加载 | 正常加载（资源重载含 `mod/jei`、JEI 图集创建），兼容插件注册无错误；`19.44+` 版本因要求 NeoForge `21.1.238+` 无法在 219 加载（已实测其拒绝信息） |
| 创造模式特殊分支删除 | 编译通过；`creative_ghost_only` 文案保留但不再被引用 |

## 与 test.10 验证的差异

- GameTest 由 68 增至 69（新增真实链路端到端）。
- 未重跑跨 JVM 回读与专服 `Done+stop`（本轮无持久化与加载链路变更；`test.10` 结果仍有效）。
- JEI 集成的游戏内拖拽仍未在自动化中覆盖，需 JEI 在环环境实测。

## 待人工复测

1. 创造模式：手持物品放物流格 / `Shift+点击` 存入，与生存行为一致。
2. 包裹自动拆包：请携带 `logs/latest.log` 重测——新诊断会记录被忽略包裹的实际地址。
3. JEI：拖物品到物流格、配方转移；同时告知 JEI 版本号。
4. 悬停物流格 tooltip（待收/空格提示文字）是否仍出现缺字。
