# v0.1.0-test.8 验证记录

- **打包日期**：2026-09-03
- **状态**：`test.7` 复测反馈的光标图标错位复制的修复版；其余复测项继承 `test.7`
- **JAR 大小**：286,119 字节
- **SHA-256**：`3feb6cb25f7ef937e0078dff68133528e3a76ea8ab0a3d1ea9781d0bac52caf2`

## 代码变更范围

仅 `InventoryCacheOverlay.renderCarriedAboveOverlay` 一个方法：光标副本的绘制锚点由 `鼠标 +10,+10` 改为原版一致的 `鼠标 -8,-8`，并补齐 z 层（+232，物品层之上）。服务端逻辑零改动。

## 自动验证

| 验证 | 结果 |
| --- | --- |
| `gradlew test build runGameTestServer` | 成功；72/72 JUnit、47/47 GameTest（含 4 项转移守恒回归） |
| GameTest 稳定性复跑 | 连续 3 次 47/47 通过 |
| 严格跨 JVM 玩家文件回读 | `-PfmpRequirePriorPlayerdata=true` 下 47/47 通过 |
| 专服启动 | `Done (1.477s)`；`stop` 正常保存退出（退出码 0） |
| JAR 元数据 | 与 `test.7` 相同口径：4 项 required、0 项 optional |

## 请人工复测

1. **光标图标**：手持物品（从格或背包取出）在物流面板上移动，应只有一个图标、跟随光标、无偏移重影；移回背包区时图标位置连续。
2. `test.7` 验证记录中的其余复测项（五项界面、四项收包、手动放取、UI 响应、总人物品量核对）在 `test.8` 上继续有效。
