# 验证记录 — v0.1.0-test.18

## 验证环境

- Java: OpenJDK 21.0.7 (java-runtime-delta)
- Gradle: 8.12, ModDevGradle 2.0.107
- Minecraft: 1.21.1, NeoForge: 21.1.219
- Create: 6.8.10, Curios: 9.5.1
- 代理: 127.0.0.1:7890 (进程级 GRADLE_OPTS)

## 验证命令

```powershell
$env:JAVA_HOME = Join-Path $env:APPDATA '.minecraft\runtime\java-runtime-delta'
$env:Path = "$env:JAVA_HOME\bin;$env:Path"
.\gradlew.bat test build --no-daemon --no-configuration-cache --console=plain
.\gradlew.bat runGameTestServer -PfmpGameTests --no-daemon --no-configuration-cache --console=plain
```

## 验证结果

### JUnit 单元测试

```
BUILD SUCCESSFUL
16 tests passed (CacheStateTest 10 + PanelLayoutTest 2 + 组数专项 1 + 其他)
0 failures
```

### GameTest 服务器

```
84 tests are now running
All 84 required tests passed :)
```

## 基线说明

- 源码直接接手自 reference/v0.1.0（test.16），而非复制重实现
- 旧 test.17 修改过的 src/ 已归档至 legacy-root-test17-src/
- 三项需求（组数计数、滑条 overlay、逐格残包绿点）在 t16 基线上做最小手术式修改
- GameTest fixture 适配沿用 test.17 已验证的数值（FULL = capacity × 64 等）
