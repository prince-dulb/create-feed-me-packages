# Create: Feed Me Packages! 0.1.0-test.24 — 验证记录

## 构建环境

- Java: OpenJDK 21.0.7（`java-runtime-delta`）
- Gradle: 8.12, NeoForge 21.1.219
- 代理: `127.0.0.1:7890`（进程级 `GRADLE_OPTS`）

## 变更摘要

私人定坠改为物主锁定：私人化写入锻造者；仅物主访问其个人缓存并可在锻造台直接连续升级；非物主佩戴即退化为全新普通坠；删除「待签收容量授权/佩戴签收」机制。`SCHEMA 7`。

## 自动测试

### JUnit（2026-09-07）

```
.\gradlew.bat test build runGameTestServer -PfmpGameTests --no-daemon --no-configuration-cache --console=plain
```

- JUnit：**15/15 通过**

### GameTest（2026-09-07，新世界 `-model8`，schema 7）

- GameTest：**85/85 通过**（含重写的 `personalUpgradeBelongsToOwnerAndAdvancesItsCache`、`personalPendantIsOwnerLockedAndDegradesForOthers`、`firstPersonalizationThenOwnerUpgradesStayOrdered`、`ownerLockedPersonalManufacturingSurvivesAnotherJvm`）

### 跨 JVM 回读（2026-09-07）

```
.\gradlew.bat runGameTestServer -PfmpGameTests -PfmpRequirePriorPersistence=true --no-daemon --no-configuration-cache --console=plain
```

- GameTest：**85/85 通过**。日志确认 `FMP_MANUFACTURING_PERSISTENCE PRIOR_JVM_PASSED schema=7 owner-lock/level-2/late-parcel/revoked/player-files` 与 `FMP_CURRENT_PERSISTENCE PRIOR_JVM_PASSED schema=7`，物主锁定、物主缓存等级、迟到包路由、撤销副本、玩家文件与缓存账本在前后 JVM 间正确回读。

## JAR 校验

```
文件: create-feed-me-packages-0.1.0-test.24.jar
大小: 300,639 字节
SHA256: D57179546783F71A1E72B0567F5C96C8C7357527C6AD5BCD6A5EFFFBB20A9F90
```

## 未执行项目

- 成品客户端截图门、缺依赖探针：未执行（沿用 test.17 起记录的缺口，建议随下一轮补验）。
- 独立专服人工 `Done`+`stop`、真实客户端启动：未执行。GameTestServer 已覆盖世界加载与保存重载、真实锻造菜单流程；等价专服/客户端验证可另行补跑。

> 说明：旧 `-model2`/`-model6`/`-model7` 测试世界含旧 schema 数据；按「不为旧测试世界建迁移、schema 变更即重开世界」的既有约定，本轮改用全新 `-model8` 世界，旧目录保留未删。旧的「授权/签收」被替代，作为历史实现事实保留在旧 release 与参考轨，仅作追溯。
