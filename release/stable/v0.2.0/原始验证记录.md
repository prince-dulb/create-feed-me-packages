# Create: Feed Me Packages! v0.2.0 — 验证记录

2026-09-09，Executor。正式 0.2.0 以 test.58 合并版 `fe42c7e` 为代码基线（P-0117 复核同意），内部版本号与文件名均为 `0.2.0`。相对 test.58 仅版本元数据变化，未改源码逻辑。

## 产物对应

| 项 | 值 |
| --- | --- |
| 提交 | `fe42c7e`（test.58 合并版；本版仅 `gradle.properties` 版本号 `0.2.0-test.58` → `0.2.0`） |
| JAR | `create-feed-me-packages-0.2.0.jar`，365,531 字节 |
| SHA-256 | `2d5cf947f4617f129b38e9dd7dbba9e045297a0bd9da41a41755a409dd64417f` |
| 用户素材 | `panel.png` SHA-256 `27c206d81c7d6324cb375aae812ee063879656190a1a454806788693ec7f7f05`（未改、未提交） |
| 网络协议 | 4 |
| 缓存 schema | 8 |

构建方式：`.\gradlew.bat clean build -x test`（`--offline --no-daemon --no-configuration-cache --console=plain`，本机 Java 21 工具链由用户级 Gradle 配置提供，仓库内不写机器路径）。

## 正式 JAR 验证（本版新增）

| 检查 | 结果与证据 |
| --- | --- |
| 成品 JAR 客户端冒烟 | 从**正式 0.2.0 JAR** 单一来源启动（`FMP_PACKAGED_SOURCE … developmentOutputs=0`）；真实窗口 `Minecraft NeoForge* 1.21.1` 渲染就绪；向该窗口发送正常关闭消息后 `Stopping!` 并退出 0；[日志](t020-packaged-client-core.log) |
| 成品 JAR 专服 | 沿用 test.58 证据：核心与全装组合均 `FMP_PACKAGED_SOURCE developmentOutputs=0` → `Done` → `stop` → 全部维度保存 → 退出 0（本版仅版本元数据变化，P-0117 同意沿用） |

## 沿用的基线证据（test.58，代码 `fe42c7e` 与正式包相同）

| 检查 | 结果 |
| --- | --- |
| JUnit | 25/25（PanelLayoutTest 8、CacheStateTest 13、ReturnServiceTest 4） |
| 核心 GameTest（21.1.219） | 116/116 |
| 双运输＋JEI418（21.1.249） | 130/130 |
| 完整开发客户端 | `FMP_CLIENT_REVIEW_PASSED`，含 `FMP_SLIDER_PRESS_IN_PLACE_PASSED`、`FMP_ADDRESS_THEN_DRAG_PASSED` 及既有地址／点击／退货场景 |
| 成品 JAR 专服（test.58 包） | 核心／全装均 Done→stop→保存→退出 0（`developmentOutputs=0`） |

完整日志与 JUnit XML 见 `release/v0.2.0-test.58/`。

## 依赖与探针

- 必需依赖仅 Minecraft、NeoForge、Create、Curios；JEI 与两个运输模组均为可选。
- 缺依赖探针沿用 test.56 的结论（依赖元数据与 test.56 实包逐项核对一致，P-0117 确认不必重跑）。
- 协议标识 4：同一存档请客户端与服务端使用同一份 0.2.0；未做混装实验。

## 玩家验收

- test.56 A—F 通过；test.57 两版 1/2/3 通过；**test.58 滑条手感用户确认正常**。
- 原“编辑地址后直接拖滑条”玩家测试已撤回：点击地址框本就会收起滑条，路径不可达（P-0117 确认，非用户欠测）。

## 如实边界

- 成品客户端冒烟覆盖**启动、渲染就绪、正常退出**；**“入世界、打开面板”未在成品 JAR 上自动完成**（成品包不含测试驱动，需 GUI 自动化）。入世界与面板交互证据来自隔离开发客户端的完整驱动（Phase 1—23），以及用户在 test.58 上的实测。
- 未测：强杀／物理断网、正式安装器目录、用户 PCL 实例、旧客户端与新服务端混装。
- 本地完成不等于公开发布；推送与 GitHub Release 需用户单独授权。
