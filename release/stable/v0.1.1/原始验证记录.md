# Create: Feed Me Packages! v0.1.1 — 验证记录

## 构建环境

- Java: OpenJDK 21.0.7（`java-runtime-delta`）
- Gradle: 8.12, NeoForge 21.1.219
- 代理: `127.0.0.1:7890`（进程级 `GRADLE_OPTS`）
- 构建目录: `../.fmp-build-safe`（`build-sandboxes` 隔离工作树 `fmp-v011-wt`，`mod_version=0.1.1`）

## 变更摘要

无主私人定坠由首个佩戴者自动认领（写入 `OWNER`），非物主佩戴仍退化为普通坠。`SCHEMA` 不变（7）。

## 自动测试

### JUnit + GameTest（2026-09-08）

```
.\gradlew.bat test build runGameTestServer -PfmpGameTests --no-daemon --no-configuration-cache --console=plain
```

- JUnit：**15/15 通过**
- GameTest：**86/86 通过**（日志确认 `FMP_MANUFACTURING_PERSISTENCE FIRST_WRITE schema=7` 与 `FMP_CURRENT_PERSISTENCE FIRST_WRITE schema=7`；Mod 列表读取 `Create: Feed Me Packages! 0.1.1`）
- 构建产物：`build-sandboxes\.fmp-build-safe\libs\create-feed-me-packages-0.1.1.jar`

### 跨 JVM 回读（2026-09-08）

```
.\gradlew.bat runGameTestServer -PfmpGameTests -PfmpRequirePriorPersistence=true --no-daemon --no-configuration-cache --console=plain
```

- GameTest：**86/86 通过**。日志确认 `FMP_MANUFACTURING_PERSISTENCE PRIOR_JVM_PASSED schema=7 owner-lock/level-2/late-parcel/revoked/player-files` 与 `FMP_CURRENT_PERSISTENCE PRIOR_JVM_PASSED schema=7 ordinary+personal exact-state/player-files`，物主锁定、物主缓存等级、迟到包路由、撤销副本、普通/个人缓存与玩家文件在前后 JVM 间正确回读。

## JAR 校验

```
文件: create-feed-me-packages-0.1.1.jar
大小: 300,569 字节
SHA256: 342E4265F66838FE74CABF3615795E16C50BBB9254C7DD6815CDCA1442FFC697
```

## 未执行项目

- 成品客户端截图门、缺依赖探针：未执行（沿用 test.17 起记录的缺口，建议随 v0.2.0 补验）。
- 独立专服人工 `Done`+`stop`、真实客户端启动：未执行。GameTestServer 已覆盖世界加载与保存重载、真实锻造菜单流程；等价专服/客户端验证可另行补跑。
- 本版本只改了 `AccessGate` 的 `owner==null` 分支 + 一个 GameTest；未触库存事务、schema 或旧交付，无迁移影响。
