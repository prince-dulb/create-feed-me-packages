# v0.1.0-test.10 验证记录

- **构建日期**：2026-09-04
- **构建产物**：`create-feed-me-packages-0.1.0.jar`
- **SHA-256**：`b08022a550dc3b30e4fc9043dcb4a291ca9bf1af8e0fd4899306e196ebdfdb2c`
- **验证执行者**：长期执行者（会话内自动化验证）；游戏内人工测试待用户执行

## 验证环境

命令进程内设置 Minecraft `java-runtime-delta`（Java 21）与进程级 `GRADLE_OPTS` 代理，未写入项目或系统配置。全部命令按 `docs/开发状态.md` "已验证命令" 表执行。

## 自动验证结果

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 单元测试 | `.\gradlew.bat test …` | 72/72 通过（协议载荷、收据／缓存记录 codec、事务内核、收包计划、持久化往返） |
| 构建 | `.\gradlew.bat build …` | 退出码 0，产物见上 SHA-256 |
| GameTest 服务器（第 1 次） | `… test build runGameTestServer` | 68/68 通过（含 5 项变体转移守恒、4 项变体键、2 项迁移夹具、6 项真实提交点弹药矩阵） |
| GameTest 稳定性复跑（第 2、3 次） | `… runGameTestServer` ×2 | 68/68、68/68 通过，无失败 |
| GameTest 跨 JVM 回读 | `… runGameTestServer -PfmpRequirePriorPlayerdata=true` | 68/68 通过；后一 Java 进程回读上一进程写出的固定 UUID schema 2 玩家文件 |
| 专服启动与退出 | `… runServer` + `help fmp` + `stop` | `Done (1.019s)`；`fmp` 命令可见；`stop` 后保存玩家与世界并正常退出（退出码 0） |
| 客户端启动 | `… runClient` | 本模组资源加载、Flywheel 79 shader、Create 初始化、声音引擎启动均正常；日志无本模组错误；稳定后人工中断（非正常退出测试） |

## 本轮覆盖的决策 0001 专项

- **变体身份与守恒**：同 ID 异组件不混算；组件-bearing 来源一步建立精确过滤并存入实物，来源原样或不残留；存款/取款/收包全部按变体守恒（GameTest＋20,000 步随机序列的 JUnit 守恒）。
- **schema 1→2 迁移**：手写 schema 1 NBT（独立于 codec）→ 迁移后 schemaVersion=2，数量 127、最低阈值 64、修订号 3、待收 32、日志 digest 全部保留；schema 2 保存/重载幂等。未知物品 ID 整记录 BLOCKED_CORRUPT，原始 NBT 保留。跨 JVM 真实存档路径回读通过。
- **T12 真实提交点**：纯查询供给 0 扣、背包有箭不动缓存；真实拉弓放箭扣 1、拉力不足 0 扣；弩装填扣 1、发射不重扣；腐烂肉等不支持物品忽略；无吊坠时缓存不可见且数据保留。弓／弩走真实 `startUsingItem`→`releaseUsingItem` 链路（useItemRemaining 经测试访问器置位）。
- **重复请求与修订号**：陈旧修订拒绝、同一缺口不重复入账、按格重置不改库存等既有 GameTest 全部在变体口径下通过。

## 待人工测试清单（游戏内）

1. 佩戴供应链坠，背包拖入普通物品／含组件物品（改名、附魔、有内容潜影盒）到物流格，确认一步成格且来源不变；同物异组件分别成格。
2. 旧存档（test.9 及更早）进入世界，确认缓存物资、格位设置与待收完整保留；日志无迁移错误。
3. 背包无箭拉弓射击、弩装填后连射，确认缓存弹药逐发扣减、取消蓄力不扣、创造模式不扣。
4. JEI ghost 拖入与配方转移（装 JEI 时）；`Shift+点击` 存入。
5. 卸下／重挂吊坠、死亡重生、退出重入后缓存与待收保持。

## 明确未执行项

- 纸飞机／运输蜂原生端到端（T08／T09）与退货阶段：不在本轮。
- TaCZ 弹药、无 Curios 护甲入口、正式序列组装：后续版本。
- 游戏内人工测试：待用户执行，上述清单仅列出建议路径。


