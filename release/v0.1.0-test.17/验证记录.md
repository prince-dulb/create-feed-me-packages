# v0.1.0-test.17 验证记录

- **构建日期**：2026-09-06
- **构建产物**：`create-feed-me-packages-0.1.0-test.17.jar`
- **SHA-256**：`8d64e720307b5cbc63ed4887b5068809d74cd3704b32252693f2f85491822119`
- **基线**：组长参考实现 test.16 移植进根工程 + 三项用户优化；schema 2 → 3（阈值按组）

## 自动验证

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| JUnit | `gradlew test` | 15/15（新增 `groupSemanticsScaleCapacityShortageAndThresholdsByNativeStackSize`；布局测试改为覆盖层契约） |
| 核心 GameTest | `runGameTestServer -PfmpGameTests=true`（NF 21.1.219） | **84/84**，复跑再次 84/84 |
| 跨 JVM 回读 | 同命令 + `-PfmpRequirePriorPersistence=true` | 84/84；`FMP_MANUFACTURING_PERSISTENCE PRIOR_JVM_PASSED schema=3`、`FMP_CURRENT_PERSISTENCE PRIOR_JVM_PASSED schema=3` |
| 双运输+JEI 组合 | `-Pneo_version=21.1.249 -PwithMobile -PwithCouriers -PwithJei -Pjei_runtime_version=19.51.0.417` | **91/91** |
| 成品专服 | `runServer -PfmpPackaged=true` + `stop` | `FMP_PACKAGED_SOURCE … developmentOutputs=0`；`Done (8.497s)` → stop 保存退出，退出码 0 |

验证用世界为各运行组合独立新建（`../.fmp-build-safe/run/gametest/<profile>`），组合矩阵含纸飞机/运输蜂真实出包、跨维度与未加载航段到货边界。

## 本轮测试修复口径

GameTest 断言按新容量语义整体换算（`ReceiveTests.FULL` = 128 组 × 64 = 8192；near-full 夹具 `FULL - 8`；单件存入到满 `FULL - 2`；源箱/请求量改为组整数倍），修复过程未改任何产品逻辑——所有域/服务/收件行为由既有测试在新数字下原样通过。

## 未执行项

- 成品客户端带截图门、缺依赖拒绝探针：本轮未跑，随下一轮补（test.15 成品门结构未变）。
- 游戏内人工复测：见变更说明"请复测"。
- 运输蜂/纸飞机串网观察项、退货（0.2.0）：不在本轮。
