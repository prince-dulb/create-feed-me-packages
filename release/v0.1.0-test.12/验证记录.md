# v0.1.0-test.12 验证记录

- **构建日期**：2026-09-05
- **构建产物**：`create-feed-me-packages-0.1.0.jar`
- **SHA-256**：`93ceb936ac307835c2b0b5e8ba674f54e7cfbac253485488f3fb3ebdc4540d8d`
- **基础**：`test.11` 之上增量修复；数据格式与 `test.10`/`test.11` 一致（schema 2）

## 根因证据（来自用户日志 `logs/latest.log`，2026-09-05 05:15–05:29 会话）

- `Package receive scan failed for PRdulb` / `IllegalStateException: Can't access registry ResourceKey[minecraft:root / minecraft:enchantment]` at `digestOfRecord` —— 缓存中含附魔书变体时，裸 NBT 编码无法解析附魔持有人引用，导致所有收包重试全部失败。
- `Caught an error from mod plugin: SupplyChainJeiPlugin … IllegalArgumentException: you cannot add a ghost ingredient handler for … [InventoryScreen, CreativeModeInventoryScreen]` —— JEI 19.51 拒绝在背包界面注册幽灵拖拽，本模组的界面处理器整体未生效。

## 自动验证

| 验证项 | 结果 |
| --- | --- |
| `test build runGameTestServer`（本轮全部修复 + JEI 基线拆分） | 退出码 0；72/72 JUnit；**70/70 GameTest** |
| GameTest 稳定性复跑 ×1 | 70/70 |
| 新增 `t07PackageReceiveWithEnchantedVariantInRecord` | 通过：缓存含附魔书变体时，石料包裹正常拆入、待收清零、书变体不受扰动（修复前该场景必现 `Can't access registry`） |
| 新增客户端 Mixin `CreativeModeInventoryScreenAccessor`（`mixins.client.json`） | 编译通过；公共类扫描（DedicatedServerClasspathTest）恢复通过 |
| 真实链路端到端 `t06RealDeliveredPackageAutoUnpacksIntoCache` | 通过；改用独立网络 UUID 后不再受 Create 跨测试网络状态影响 |
| JEI 版本策略 | 编译基线 19.51.0.418（对齐用户环境 19.51.0.417）；开发实例 localRuntime 19.21.0.246（NeoForge 219 可加载）已验证启动 |

## 未执行项

- 游戏内人工复测（见变更说明"请复测"）。
- 专服 `Done+stop`、跨 JVM 回读、`runClient` 启动：本轮无持久化/加载链路变更，`test.10` 结果仍有效；JEI 相关仅在开发实例验证加载。
