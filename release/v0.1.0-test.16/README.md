# 0.1.0-test.16 本地测试包

[安装JAR](create-feed-me-packages-0.1.0-test.16.jar)。来源为reference/v0.1.0，旧包不覆盖；只安装一个FMP版本，不自动操作你的实例。

本轮只修改客户端界面：地址写入顶部横幅，点击复制完整地址、超长显示截短；升级优先横向扩为2／3／4／5／6列，窄屏减少列并保留滚动；存在真实残包时左上角显示4×5像素缓慢闪亮的小点。未改库存、收件、制造事务或schema。

**按用户要求仅编译打包，未执行自动测试、游戏启动或成品运行验收；不保证没有界面问题。test.17恢复完整验证，并补验本轮布局和交互。**

依赖：Minecraft1.21.1／NeoForge21.1.219及以上、Create6.0.10（低于6.1.0）、Curios9.5.1（低于10.0.0）。JEI和两种运输模组均非依赖声明，本轮未重新联调。沿用test.15运行环境不代表test.16已测。

请检查：①长地址不越框、点击复制内容完整；②1—5级横向扩列、滚动末格和选中格滑条；③有残包时小点发亮，收空后消失。尤其试你常用GUI缩放及JEI／配方书同时打开的情况。原有存取与合成若异常请停用并反馈。

参考工程正确构建日志：安全输出reports/reference/build-test16-reference.log，jar任务17秒、退出0，有两条既有API弃用警告。首次错误地从根目录调用Gradle，任务均UP-TO-DATE，其结果不计入本包证据；未读取／复制根源码，不采用根产物。
