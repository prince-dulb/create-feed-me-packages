# 开发状态

**当前交付为 v0.1.0（首个本地可玩版本）**：基于 test.24 代码（私人定坠物主锁定、残包按物品格分格、精确组件身份、真实补货/收件口、合成/JEI/标准投射物），`SCHEMA 7`，必需依赖契约核对通过。用户已确认 test.24 与生存档无问题，据此建立 `release/v0.1.0/`。验证记录见 [v0.1.0 验证记录](release/v0.1.0/验证记录.md)。

- **目标版本**：`v0.1.0`（已发布）→ 之后 0.2.0 退货、0.3.0 插件
- **最后更新**：2026-09-07
- **授权状态**：2026-09-07 用户确认 test.24 与生存档通过，授权作为 v0.1.0 本地交付；公开发布另需授权
- **当前任务**：v0.1.0 已本地交付；剩余未跑门（成品客户端截图、缺依赖探针运行、默认专服 agent 附加验收）在验证记录中如实披露；下一轮按路线图进入 0.2.0 真实退货

## 授权边界

此前“开始做第一版”的授权允许在本项目内创建 NeoForge 工程、源码、资源、测试、Gradle Wrapper 和隔离运行实例，并通过 Gradle 解析锁定依赖；未来工作改由 [plan-v2.0](开发计划书/README.md) 分期。该旧授权不自动覆盖 2026-09-05 新缓存模型的 schema、注册 ID 与数据格式替换。

2026-09-04 用户确认以“物品注册 ID＋完整持久化组件”作为缓存精确变体身份，并授权将已有玩家缓存从 schema 1 无损迁移至 schema 2。该授权仅覆盖[决策 0001](decisions/0001-精确组件物品身份.md)和[规格 01](开发计划书/specs/01-个人缓存与持久化.md)定义的迁移；不授权删除旧玩家文件、静默丢弃坏字段或借迁移执行其他数据变更。

2026-09-05 用户确认的本件缓存／私人定坠模型会再次改变缓存所有权、持久化索引和包裹路由。项目尚未发布首个正式版本，因此新模型直接使用干净 schema 与新注册 ID，**不沿用 schema 1→2 兼容代码，也不新增旧测试数据迁移、别名注册或双格式解码**。旧测试世界允许失效但不得由代理删除。该方向允许主策继续编写规格、允许执行者只读研究入口；在干净 schema、失败原子性和验证条件写明并由用户批准实施前，不得修改现有源码或数据格式。

2026-09-05 用户随后明确委托 @Planner 独立编写一套 `v0.1.0` 参考实现，并要求按“即将发布版本”的质量标准完成。该授权解除的只是 `reference/v0.1.0/` 干净实现轨；@Planner 可以在此目录设计并实现新 schema、注册项与技术细节，但不得读取或改动根工程实现。根源码轨仍须由长期执行者按正常流程处理，参考实现不会自动合并进去。

本轮不授权删除文件／目录或 Git 历史，不授权全局安装、系统配置、密钥与 `.env`、CI/CD、Git 推送／历史重写、公开发布。是否初始化本地 Git 仓库不是 T01 的必要条件，本阶段不做。

## 任务状态

下表保留旧 T00—T15 的实际完成事实，**不是 plan-v2.0 的未来执行队列**。新工作采用[版本路线图](开发计划书/版本路线图.md)中的 R0—R6；对应实现尚未开始。

| 任务 | 状态 | 已有证据 | 下一出口 |
| --- | --- | --- | --- |
| T00 实施授权与执行边界登记 | 完成 | 本文件、`AGENTS.md` 阶段与目录规则已更新 | 进入 T01 |
| T01 NeoForge 工程骨架 | 完成 | 工程、Wrapper、锁定依赖、空载入口、JUnit 与 GameTest 已建立；C0 客户端／专服／GameTest Server 通过；Create／Curios 缺失拒绝通过 | 证据见 [T01 工程骨架与 C0 验证](research/2026-08-30-T01工程骨架验证.md) |
| T02 注册、数据组件与网络协议 | 完成（test.10） | 协议版本 4：`ItemVariantKey`（物品＋完整组件）网络/NBT codec、`SetFilterPayload` 模板化（含 6 个 600 字符组件模板的 36 格 S2C < 64 KiB 证明）、收据组件惰性持久化 codec、72/72 JUnit；旧证据见 [T02 注册与协议验证](research/2026-08-30-T02注册与协议验证.md) | —（后续变更走变更记录） |
| T03 个人缓存与事务内核 | 旧模型完成（test.10），新模型重开 | schema 2 + `ItemVariantKey` 全事务及 schema 1→2 测试是已执行历史；见[规格 01](开发计划书/specs/01-个人缓存与持久化.md) | 为本件／个人缓存设计干净新 schema；不再补测或保留旧测试存档迁移 |
| T04 供应链坠与网络绑定 | 完成 | 实例 UUID、Curios `necklace`／`curio` 有效槽枚举、真实 Stock Link 绑定、清绑／禁用、稳定归并、网络可用门、重复 ID 修复和转交隔离已实现；51/51 JUnit、15/15 GameTest 的完整回归通过 | 证据见 [T04 供应链坠与网络绑定验证](research/2026-08-31-T04供应链坠与网络绑定验证.md) |
| T05 物流面板与五阶段交互 | 受影响部分完成（test.12） | 变体口径交互 + 两个层级修复（滑条面板抬到物品图标之上；设置面板打开期间不发格位 tooltip）+ 创造按标签页区分（生存标签页一致、创造标签页 ghost） | 布局与手感待用户游戏内复测 |
| T06 补货请求与本地收包 | 受影响部分完成（test.10） | 缺口、待收与请求全部按 `ItemVariantKey`；待收新增"变体须与来源格过滤一致"校验；自动回归通过 | 与 T08/T09 原生运输的端到端联调未做 |
| T07 本地包裹接收与残包 | 受影响部分完成（test.12） | 变体口径下切点守恒等 70/70 GameTest；两个实战阻断已修：收据惰性 codec（test.10）与收包日志摘要裸 NbtOps 编码（用户日志定位：附魔变体致 `Can't access registry`，test.12 改注册表感知并加回归测试）；另加地址不符包裹诊断日志 | 游戏内复测随 test.12 清单 |
| T08 纸飞机原生联调 | 证据整理中，无适配器代码 | 按 P-0008 取消适配器与目标登记要求；第三方事实已备（锁定 JAR SHA-1 `54efc43f…`、地址解析、交付条件、配置门，见 [T08 研究记录](research/2026-09-02-T08纸飞机只读研究准备.md)）。用户环境已装 Mobile Packages；已实测：Create 请求产出真实包裹、手动取包裹后 T07 自动拆包。**未覆盖**：纸飞机原生端到端（Location Transmitter 登记＋零件装包＋投掷/弹射器→完整包入背包→自动收包）、预拆模式诊断 | 整理已有证据、列补测清单交用户实测；发现核心交接缺陷按规格修 |
| T09 运输蜂原生联调 | 未开始 | 用户环境已装 Mobile Packages；蜂成员条件、完整包入背包、满背包行为均未实测 | T08 之后同法整理与补测 |
| T10 原版配方书取料 | 受影响部分完成（test.10） | 候选、模拟与提交按精确组件变体执行；8 项守恒测试在变体口径下通过 | 游戏内复测随 test.10 清单 |
| T11 JEI 过滤与配方转移 | 受影响部分完成（test.12） | JEI 19.51 禁止背包界面 ghost 注册（用户日志证实启动即抛错）——已移除该注册，改为**中键过滤选择模式**（经 IJeiRuntime 取鼠标下物品，拖到物流格设过滤）；工作台 ghost 保留；编译基线 19.51.0.418，开发实例 localRuntime 19.21.0.246 | 用户复测中键选过滤流程 |
| T12 弓弩弹药取用 | 完成（test.10，按规格 06 重写） | 查询零扣（脱离副本）、`ProjectileWeaponItem.draw` 单 Mixin 真实提交点：弓放箭单扣、拉力不足零扣、弩装填单扣且发射不重扣、无限／创造不扣；6 项 GameTest 走真实 startUsingItem→releaseUsingItem 链路 | 游戏内手感与第三方武器实测待做 |
| T13—T15 | 阻塞 | 旧 `test.9` 的 72/72 JUnit、61/61 GameTest 是已执行事实，但不能关闭新规格 | 完成重开队列、T08／T09 原生联调、系统矩阵后再进入最终本地交付 |

## 环境事实

| 项 | 当前事实 | 边界 |
| --- | --- | --- |
| 系统 PATH Java | Microsoft OpenJDK 17.0.19 | 不能用于 Minecraft 1.21.1 编译目标 |
| 可用 Java 21 | Minecraft Runtime `java-runtime-delta`，OpenJDK 21.0.7 | 仅在命令进程内设置 `JAVA_HOME`，不改系统环境变量 |
| Gradle | 项目 Wrapper 8.12 已验证；ModDevGradle 2.0.107 | 使用项目 Wrapper，不全局安装 |
| Git | 2.54.0.windows.1 | 当前未初始化仓库，也未获 push／历史重写授权 |
| 生成目录 | `../.fmp-build-safe/` | 项目名含 `!`，默认项目内 `build/` 会使当前 SecureJar 运行链漏发现 NeoForge；实测记录见 T01 验证 |
| 模组依赖元数据 | Minecraft／NeoForge、Create、Curios 共 4 项，均为 required | JEI、纸飞机、运输蜂不声明 required／optional；版本只进入兼容测试矩阵 |

## 已验证命令

以下命令均曾在 PowerShell 中执行。当前机器先对**单个命令进程**设置 Java 21；因本机网络代理不会被 Java 自动继承，还设置了进程级 `GRADLE_OPTS`。这些值没有写入项目或系统配置。2026-09-03 的成功结果验证旧 schema／默认模板实现；它们尚未验证 2026-09-04 的精确组件与迁移规格，且其中 T12 测试的期望本身错误：

```powershell
$env:JAVA_HOME = Join-Path $env:APPDATA '.minecraft\runtime\java-runtime-delta'
$env:Path = "$env:JAVA_HOME\bin;$env:Path"
$env:GRADLE_OPTS = '-Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=7890 -Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7890'
```

| 命令 | 结果 | 证据边界 |
| --- | --- | --- |
| `.\gradlew.bat test build runGameTestServer --no-daemon --no-configuration-cache --console=plain` | 退出码 0；构建、72/72 JUnit、**61/61 GameTest** 通过（2026-09-03 `test.9` 轮次）。其中 T12 测试把查询扣料当成功，只能证明现实现一致，不能证明规格正确 | `../.fmp-build-safe/reports/tests/test/index.html`、`run/gametest-c0/logs/latest.log`；作为旧模型回归基线 |
| `.\gradlew.bat test build runGameTestServer --no-daemon --no-configuration-cache --console=plain`（2026-09-04 `test.10` 轮次，schema 2 + 变体键实现） | 退出码 0；72/72 JUnit、**68/68 GameTest** 通过；同日 `runGameTestServer` 稳定性复跑 2 次均 68/68 | `../.fmp-build-safe/reports/tests/test/index.html`、`run/gametest-c0/logs/latest.log` |
| `.\gradlew.bat runGameTestServer -PfmpRequirePriorPlayerdata=true …`（2026-09-04 `test.10` 轮次） | 退出码 0；68/68 通过；后一 Java 进程回读上一进程写出的固定 UUID **schema 2** 玩家文件 | `run/gametest-c0/world/playerdata/` |
| `.\gradlew.bat runServer …` + `help fmp` + `stop`（2026-09-04 `test.10` 轮次） | `Done (1.019s)`；`fmp` 命令注册可见；`stop` 保存后正常退出（退出码 0） | `run/server-c0/logs/latest.log` |
| `.\gradlew.bat runClient …`（2026-09-04 `test.10` 轮次） | 资源重载、Flywheel 79 shader、Create 初始化、声音引擎启动正常，日志无本模组错误；稳定后人工中断 | `run/client-c0/logs/latest.log` |
| `runGameTestServer` 稳定性复跑 | `test.9` 轮次连续 3 次 61/61 通过 | `run/gametest-c0/logs/latest.log` |
| `.\gradlew.bat runGameTestServer -PfmpRequirePriorPlayerdata=true --no-daemon --no-configuration-cache --console=plain` | 退出码 0；后一 Java 进程回读上一进程的固定 UUID 压缩 schema 1 玩家文件，61/61 GameTest 通过 | `t03CompressedPlayerDataFileRoundTripsThroughServerStorage`、`run/gametest-c0/world/playerdata/`；schema 2 迁移跨 JVM 回读仍待执行 |
| `.\gradlew.bat runServer --no-daemon --no-configuration-cache --console=plain`，启动后执行 `help fmp`、`stop` | 退出码 0；专服到达 `Done (1.105s)`，`stop` 后保存世界并正常退出（2026-09-03 `test.9` 轮次；历史轮次 0.723s／1.512s） | `run/server-c0/logs/latest.log` |
| `.\gradlew.bat runClient --no-daemon --no-configuration-cache --console=plain` | 本模组资源参与重载；完成声音、纹理、Flywheel 79 个 shader 与 Create 初始化；日志没有本模组错误或缺失模型；稳定后人工中断 | `run/client-c0/logs/latest.log`；退出码 1 来自关闭方式，不当作正常退出测试 |
| `ProtocolPayloadTest` 对旧协议每种最大载荷编码、解码 | 最大 C2S 269 B；36 格＋64 终端最大初始 S2C 12,182 B，分别低于 8 KiB／64 KiB；未包含精确组件模板 | `../.fmp-build-safe/test-results/test/TEST-dev.scathiard.feedmepackages.network.ProtocolPayloadTest.xml`；新协议须重测 36 个近 1,024 B 模板 |
| `DedicatedServerClasspathTest` 扫描主类常量池 | 公共主类无 `net/minecraft/client` 或 `mezz/jei` 引用 | JUnit 报告；客户端屏幕只从 `Dist.CLIENT` 入口注册 |
| `.\gradlew.bat dependencies --configuration runtimeClasspath --no-daemon --no-configuration-cache --console=plain` | 退出码 0 | `../.fmp-build-safe/reports/t01/runtimeClasspath.txt` |
| 使用 `gradle/verification/exclude-runtime-mod.init.gradle` 分别排除 Create／Curios 后运行专服 | 两次均由加载器明确报告本模组缺少对应必需依赖 | `../.fmp-build-safe/reports/t01/missing-create.txt`、`missing-curios.txt`；加载器捕获失败后 Gradle 可能仍返回 0，按诊断文本验收 |

## 当前验证边界

截至 2026-09-05 15:48，@Planner 的干净参考轨已在 `reference/v0.1.0/` 建立独立 Gradle 8.12／ModDevGradle 2.0.107 工程、最小 `@Mod` 入口和固定依赖矩阵；以 Minecraft Runtime OpenJDK 21.0.7 运行 `compileJava --no-daemon --no-configuration-cache --stacktrace`，退出码 0，`BUILD SUCCESSFUL in 33s`。该结果只证明干净构建基线，尚未实现 schema、注册物品、缓存、界面、物流、取料或升级，也未运行 JUnit、GameTest、客户端、专服和第三方联调，**没有可玩 JAR，也没有创建 `release/v0.1.0-test.13/`**。用户随后要求暂停，当前不继续编码。

2026-09-04 的历史边界：决策 0001（精确组件物品身份）与 schema 1→2 迁移已实现，T12 已按当时[规格 06](开发计划书/specs/06-弓弩弹药取用.md)重写为“查询零扣＋真实消耗点单扣”；`release/v0.1.0-test.10` 已交付并附验证记录。T08／T09 原生运输联调与退货阶段未开始。本轮实现过程中的审查与检查点见[组件身份与 T12 技术审查](research/2026-09-04-组件身份与T12技术审查.md)与 `docs/团队通信.md` E-0022。

以下段落按日期保留旧轮次事实；若与上面的当前边界冲突，以 2026-09-05 plan-v2.0 状态为准。

截至 2026-09-02，用户已通过 `test.4` 验证记录中的全部旧待测项；这关闭了创造模式入口、最后一组取出、Curios 重装及既定布局复测，但不代表新发现的缺陷已修复。新记录显示：状态箭头和设置点被物品图层遮挡；来源优先级和升级件缺少足够说明；创造目录物品发送 ghost 过滤后被服务端以 `FEATURE_NOT_READY` 拒绝；空过滤格缺少易发现的清除动作。详细事实与修复边界见[2026-09-02 test.4 人工验收](research/2026-09-02-test4人工验收.md)。

`test.5` 已实现对应修复并通过自动回归和客户端资源启动，但渲染层级、光标偏移、tooltip 可读性与 `Shift+右击` 的实际手感只能由真实客户端人工确认，因此 T05 继续保持“验收中”。

T06 已完成补货请求侧的首个闭环：仅按缓存数量与当前待收计算缺口，普通背包、快捷栏和副手不参与最低值判断；有效饰品每 20 tick 评估一次。自动测试已通过真实 Create 箱子、打包机和 Stock Link 抽取库存并生成 `FMP@玩家名` 实体包，用户也已观察到请求成功发出并有包裹抵达玩家。Create 派发调用一旦开始后若抛错，结果可能已部分生效，因此保留待收并依赖按格手动重置，不自动回滚或重发。抵达包裹的本地处理已由 T07 收包侧接管，见下一段。

T07 收包侧核心已于 2026-09-02 实现并通过自动回归（`release/v0.1.0-test.6`）：主物品栏 0—35 槽有界扫描只处理地址精确等于 `FMP@当前玩家名` 的完整包裹；第一次观察写入收据并按提交时间从旧到新一次性结算同物同修订待收；部分入库按各格实际容量守恒转移，余货连同额外组件留包；残包在腾位后（取出提交后立即触发或周期扫描兜底）续收且不二次结算；每次收包写一条 PREPARED→COMMITTED 整包日志，非终态日志封锁自动收包，256 条上限触发背压；畸形堆叠、超过 9 槽、外来收据与错误地址整包不动。修复过程中发现并解决了三个 T04 测试的 Create 全局物流行为跨波次泄漏（测试基建问题，不影响产品）。`SPK-PKG-01` 的四切点故障注入重载守恒已于同日由恢复会话完成：上个会话写出的切点测试文件经修复（缺导入、NeoForge 21.1.219 的 `Entity#setData` 为 final 故改注入附件 Map、两处测试期望错误）后，PREPARED 后、包裹减少后、缓存增加后、COMMITTED 后四切点全部守恒，非终态日志只封锁不清理；产物 JAR 与 `test.6` 逐字节一致（SHA-256 `27d033d4…`），验收面未破坏。T07 自动验证门槛全部满足，剩余游戏内人工复测四项见 test.6 验证记录。详情见 [T07 切点故障注入验证](research/2026-09-02-T07切点故障注入验证.md)与 [T07 收包侧验证](research/2026-09-02-T07收包侧验证.md)。

2026-09-03 用户在 `test.6` 上的人工测试报告四项反馈：手动放入物品损坏、"背包有货仍判断缺货"、疑似刷物品序列、从 Curios 界面返回后面板迟到约 1 秒。定位结果：服务端补货派单／收包／升级会推进缓存修订号但从不主动推送快照，客户端点击携带旧修订号被判过期静默失败（"手动放入损坏"与 UI 迟滞的共同根因）；"疑似刷物品"的净结果由用户复测证实在 `test.7` 上消失，当时的解释（补货回填／手动取包裹／双击收集）按[首轮技术审查](research/2026-09-03-首轮技术审查.md)口径为**候选解释**——确定共同因素是显示滞后，逐帧因果不再单独主张；"背包有货仍判断缺货"是已确认规格（补退货只看缓存内数量），未改动，如需修订由用户与主策划决定。修复以 `release/v0.1.0-test.7` 交付：三处服务端提交点推送快照、客户端无快照 150ms 即时请求、新增转移守恒回归，47/47 GameTest、72/72 JUnit、跨 JVM 回读与专服正常退出通过。`test.8` 修复光标图标错位复制（仅客户端渲染），用户复测确认"没问题了"。**证据口径**：守恒回归直接调用服务层与意图入口，证明这些入口在给定输入下封顶且守恒；鼠标事件转换、网络载荷编解码与客户端菜单／光标同步的完整链路未在自动化覆盖内。2026-09-03 按审查补 `t05HostilePayloadsThroughIntentServiceConserve`：敌意计数（`Integer.MAX_VALUE`／负数）经 `CacheIntentService.handle` 真实入口封顶或拒绝（48/48 GameTest）。复测清单见 `release/v0.1.0-test.7/验证记录.md` 与 `test.8` 记录。

2026-09-02 按用户最新决定修订了尚未实施的 T12：不按原版弓／弩物品 ID 特判，而是计划接入 `LivingEntity#getProjectile` 与 `ProjectileWeaponItem.draw/useAmmo` 的共享投射物查询／消费协议，并建立可供其他消费适配器复用的统一服务。本地锁定 JAR 与 NeoForge sources 已完成静态核查；T12 代码、非原版测试子类、客户端和游戏内验证均未开始，不能把协议可用性写成兼容通过。

2026-09-01 按用户最新决定移除了 JEI、Package Couriers 与 Create: Mobile Packages 的全部加载器依赖声明，也移除了尚未使用的 JEI Maven 仓库、版本属性和 `compileOnly` API。资源契约测试固定生成后的 `neoforge.mods.toml` 只能有 Minecraft／NeoForge／Create／Curios 四项 required 依赖且不得出现 optional；这证明新 JAR 不会再因本模组的 JEI 版本范围拒绝加载，但不等于 JEI UI／ghost 已完成联调。证据见[兼容依赖声明修订](research/2026-09-01-兼容依赖声明修订.md)。
